<tr id="<%= post.id %>">
  <td><%= post.title %></td>
  <td><%= link_to 'Show', post, class:'btn btn-info btn-sm' %>
  <%= link_to 'Edit', edit_post_path(post), class:'btn btn-warning btn-sm' %>
  <%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?', remote: true }, class:'btn btn-danger btn-sm' %></td> <!-- Agregar archivo destroy.js.erb, remote: true y format.js en método destroy en el Controller -->
</tr>